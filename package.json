{
  "version": "1.0.0",
  "main": "dist/server.js",
  "scripts": {
    "start-local": "ENVIRONMENT=local tsc-watch --onSuccess \"lambda-local -l dist/server.js -h handler --watch 8008\""
  },
  "author": "org_name",
  "license": "ISC",
  
  "dependencies": {
    "chrome-aws-lambda": "^10.1.0",
    "puppeteer-core": "^10.1.0",
    "dotenv": "^16.0.2"
  },
  "devDependencies": {
    "@types/node": "^18.7.21",
    "lambda-local": "^2.0.3",
    "tsc-watch": "^5.0.3",
    "typescript": "^4.8.3"
  }
}
